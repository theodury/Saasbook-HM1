# This "master Gemfile" contains gems used by the various ESaaS homeworks.
# Extract a list of install commands by running bundler and then:
# bundle list | perl -ne 'print "gem install --no-rdoc --no-ri $1:$2\n" if /([-_A-Za-z]+)\s+\((\S+)\)/' | sort

source 'https://rubygems.org'

ruby '1.9.3'


gem 'bundler', '1.7.6'

gem 'autotest-rails' 


group :development, :test do
  gem 'rspec-rails', '~> 3.0.0'
end